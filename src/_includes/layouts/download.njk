---
layout: layouts/main.njk
eleventyComputed:
  hero:
    title: "Downloads"
    description: "Download Prism Launcher {{version.current}}"
---
<style>
  .background {
    filter: brightness(80%);
  }
</style>

{{ collections.download | eleventyNavigation | eleventyNavigationToHtml({ activeKey: eleventyNavigation.key, listClass: 'tab-list', activeListItemClass: 'tab-active' }) | safe }}

{{ content | safe }}
